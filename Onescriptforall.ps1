#! /usr/bin/env bash
echo "You can do very limited stuff in both bash and powershell with no code duplication:"
echo "You can invoke executables with parameters, and you use echo, and some stuff like ls"
echo "The shebang header will make it runn in bash on nix"
echo "The .ps1 file extension will make it run in powershell on Windows"